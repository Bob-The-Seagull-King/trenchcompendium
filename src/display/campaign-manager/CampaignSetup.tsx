import React from 'react';

const CampaignSetup: React.FC = () => {
    return (
        <div className="CampaignSetup">
            <h1 className="text-xl font-bold">Campaign Setup</h1>
        </div>
    );
};

export default CampaignSetup;